#Rename this to settings.py and provide an api_key

#api_key="...insert some random key here..."

#Enable these settings when running in a SmartOS zone with mod_wsgi
#import os
#os.environ["MAGICK_HOME"]="/opt/local"
#os.environ["PATH"]+=":/opt/local/bin"

db_host="db"
